apiVersion: apps/v1
kind: Deployment
metadata:
  name: hotel
spec:
  replicas: 1
  selector:
    matchLabels:
      service: hotel
  template:
    metadata:
      labels:
        service: hotel
    spec:
      containers:
        - name: hotel
          envFrom:
            - configMapRef:
                name: saga-demo-conf
          image: HOTEL_IMAGE
          resources:
            requests:
              cpu: 125m
              memory: 128Mi
          ports:
            - containerPort: 8090
