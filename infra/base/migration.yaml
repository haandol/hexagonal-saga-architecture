apiVersion: batch/v1
kind: Job
metadata:
  name: migration
spec:
  ttlSecondsAfterFinished: 100
  template:
    spec:
      serviceAccount: migration-service-account
      containers:
      - name: migration
        image: MIGRATION_IMAGE
        imagePullPolicy: Always
      restartPolicy: Never
