apiVersion: apps/v1
kind: Deployment
metadata:
  name: flight
spec:
  replicas: 1
  selector:
    matchLabels:
      service: flight
  template:
    metadata:
      labels:
        service: flight
    spec:
      containers:
        - name: flight
          envFrom:
            - configMapRef:
                name: app-conf
          image: FLIGHT_IMAGE
          resources:
            requests:
              cpu: 125m
              memory: 128Mi
          ports:
            - containerPort: 8090
