apiVersion: apps/v1
kind: Deployment
metadata:
  name: trip
  app.kubernetes.io/type: app
spec:
  template:
    spec:
      containers:
        - name: trip
          envFrom:
            - configMapRef:
                name: app-conf
            - configMapRef:
                name: aws-conf