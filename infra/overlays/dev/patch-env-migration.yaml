apiVersion: batch/v1
kind: Job
metadata:
  name: migration
  app.kubernetes.io/type: batch
spec:
  template:
    spec:
      containers:
        - name: migration
          envFrom:
            - configMapRef:
                name: app-conf
            - configMapRef:
                name: aws-conf